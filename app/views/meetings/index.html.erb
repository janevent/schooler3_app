<% if params[:course_id] %>
    <h2><%= @course.title %></h2>
<%end%>
<% @meetings.each do |meet| %> 
    <%= meet.start_time.strftime( "%l %M %p") %>
    <%= meet.end_time.strftime( "%l %M %p") %>
<%end%>  
<h3>Monday</h3>
<% if !@monday_meetings.empty? %>
    <% @monday_meetings.each do |day| %>
        <% day.meetings.each do |meet| %>
            <p>Start Time:<%= meet.start_time.strftime( "%l %M %p")%>  End Time: <%= meet.end_time.strftime( "%l %M %p") %></p>
        <%end%>
    <%end%>
<%end%>
<h3>Tuesday</h3>
<h3>Wednesday</h3>
<h3>Thursday</h3>
<h3>Friday</h3>
<h3>Saturday</h3>
<h3>Sunday</h3>